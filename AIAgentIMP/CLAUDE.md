# 项目指令（CLAUDE.md）

## 目标
- 优先正确性与最小改动。

## 工作约定
- 先读后改，不要凭空猜测。
- 变更前说明影响范围，避免大重构。

## 运行与测试
- 修改后尽量跑对应测试。
- 常用命令示例：`pytest` / `pnpm test` / `npm test`

## 禁止事项
- 不改 `vendor/`、`generated/` 等自动生成目录。
- 不升级依赖或改构建脚本，除非明确要求。

## 输出要求
- 简要说明做了什么、为什么。
- 需要时补充注释或更新文档。
